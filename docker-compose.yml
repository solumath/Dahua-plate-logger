services:
  plate_logger:
    build: .
    restart: always
    volumes:
      - ./config:/config:ro
      - ./data:/data
    environment:
      TZ: Europe/Prague
      # DB_DIR: /data/plates
      # LOG_DIR: /data/logs
      # LOG_LEVEL: info
      # PORT: 8080
    ports:
      - "80:8080"
